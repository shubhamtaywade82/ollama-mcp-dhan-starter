# === Ollama ===
OLLAMA_URL=http://localhost:11434
LLM_MODEL=llama3.1:8b-instruct-q5_K_M

# === Mode ===
# MODE=rails (preferred) -> MCP tools call your Rails executors (you expose REST endpoints)
# MODE=dhan              -> MCP tools call DhanHQ API directly
MODE=rails

# === Rails executors (used when MODE=rails) ===
RAILS_EXECUTOR_URL=http://localhost:3000
RAILS_API_KEY=changeme

# Expected endpoints on Rails (you implement):
#  GET  /llm/funds
#  GET  /llm/positions
#  GET  /llm/orders
#  GET  /llm/spot?underlying=NIFTY
#  GET  /llm/quote?securityId=12345
#  GET  /llm/option_chain?underlying=NIFTY&expiry=2025-09-11
#  POST /llm/place_bracket_order {securityId, qty, sl_pct, tp_pct}
#  POST /llm/modify_order {orderId, ...}
#  POST /llm/cancel_order {orderId}

# === DhanHQ (used when MODE=dhan) ===
DHAN_BASE_URL=https://api.dhan.co
DHAN_ACCESS_TOKEN=your_dhan_token
DHAN_WS_URL=wss://your_dhan_ws_endpoint

# === Master Script CSV ===
MASTER_CSV=./data/dhan_master_script.csv
